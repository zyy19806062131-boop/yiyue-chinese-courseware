services:
  - type: web
    name: yuyin-tone-trainer
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    healthCheckPath: /healthz
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4
      - key: TRAINER_DATA_JSON
        sync: false
      - key: ACCESS_CODES_JSON
        sync: false
